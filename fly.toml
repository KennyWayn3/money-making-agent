app = "money-making-agent"
primary_region = "iad"

[build]
  builder = "heroku/builder:22"
  buildpacks = ["heroku/nodejs"]

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]
